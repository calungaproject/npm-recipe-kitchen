# fullsend per-repo configuration
# https://github.com/fullsend-ai/fullsend
#
# This file configures fullsend for per-repo installation mode.
# See ADR 0033 for details.
version: "1"
kill_switch: false
# The custom npm-recipe-draft harness dispatches as role `coder`, so that role
# must be declared here for the mint service to issue its token.
# Stock agents (triage/retro/etc.) remain intentionally stripped.
roles:
    - coder
agents:
    - name: npm-recipe-draft
      source: harness/npm-recipe-draft.yaml
allowed_remote_resources:
    - https://raw.githubusercontent.com/fullsend-ai/fullsend/
    - https://raw.githubusercontent.com/fullsend-ai/agents/
create_issues:
    allow_targets:
        repos:
            - calungaproject/npm-recipe-kitchen
            - fullsend-ai/fullsend
