# Recipe draft (review only)

This PR stages a **draft** recipe for `chalk@6.0.0`. It is **not** an npm-registry onboarding PR.
Human review is required before promoting files to `calungaproject/npm-registry`.

## Reason for needs_human

Facts unavailable: source repository host is unsupported or unparseable: "chalk/chalk" (reason_code: UNSUPPORTED_REPO_HOST). Cannot draft recipe without trusted source URL and package metadata.

## Next steps

1. Review manifest, build entrypoint, and verify scripts (if present).
2. Fix gaps and re-run `/fs-onboard chalk@6.0.0` or manually promote to npm-registry.
